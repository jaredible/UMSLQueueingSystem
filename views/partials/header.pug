nav(class='navbar navbar-expand-lg navbar-dark navbar-fixed-top bg-dark')
  a(class='navbar-brand', href='/')
    img(src='/img/brand.png', width='30', height='30', class='d-inline-block align-top', alt='')
    |  AQS
  button(type='button', class='navbar-toggler', data-toggle='collapse', data-target='#navbarNavDropdown')
    span(class='navbar-toggler-icon')
  div(id='navbarNavDropdown', class='navbar-collapse collapse')
    ul(class='navbar-nav mr-auto mt-2 mt-lg-0')
      li(class='navbar-nav active')
        a(class='nav-link', href='/')
          | Home
      li(class='navbar-nav')
        a(class='nav-link', href='/account/reservations')
          | Reservations
    form(id='formLogout', class='form-inline my-2 my-lg-0', action='/account/logout', method='post')
      button(type='submit', class='btn btn-logout my-2 my-sm-0') Log out
